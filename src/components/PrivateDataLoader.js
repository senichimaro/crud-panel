import React, { useState, useEffect } from 'react'

export const PrivateDataLoader = ({ getF = ()=>{}, userID, children}) => {
    const [state, setState] = useState([])

    (() => {
        (async () => {

        })()
    },[])
    return 1
}